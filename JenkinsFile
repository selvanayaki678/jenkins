@Library('my-shared-library') _
pipeline {
    agent any
    stages {
      stage('print hello') {
        hello()
      }
    stage('print hello') {
        printName("selva","Friday")
      }
    }
}
